import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Certifique-se de que isso está correto
import './index.css'; // Importe o CSS do Tailwind


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
